# yarn-v1-lock-snip

Remove a package from a yarn v1 lockfile.

```
npx @ymainier/yarn-v1-lock-snip yarn.lock <package-name>
```

Removes all version entries for `<package-name>` from `yarn.lock`, editing the file in place.
